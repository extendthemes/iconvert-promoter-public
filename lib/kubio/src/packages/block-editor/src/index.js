import * as KubioBlockEditor from './gutentag-block-editor';

// eslint-disable-next-line no-undef
wp.blockEditor = KubioBlockEditor;
window.isKubioBlockEditor = true;

export default KubioBlockEditor;
