const durationOptionsConfig = {
	ms: {
		min: 500,
		max: 10000,
		step: 100,
	},
};

const speedOptionsConfig = {
	ms: {
		min: 0,
		max: 10000,
		step: 100,
	},
};

export { durationOptionsConfig, speedOptionsConfig };
